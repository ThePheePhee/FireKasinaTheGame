import type {GameStats} from '../game/gameState';
import './GameMode.css';

const meters:[keyof GameStats,string][]=[['concentration','CONCENTRATION'],['clarity','CLARITY'],['confusion','CONFUSION']];
export default function GameHud({stats,mistOverride,fairyOverride,onMistOverride,onFairyOverride}:{stats:GameStats;mistOverride:boolean;fairyOverride:boolean;onMistOverride:(checked:boolean)=>void;onFairyOverride:(checked:boolean)=>void}){return <aside className="game-hud" aria-label="Player meters">{meters.map(([key,label])=><div className={`meter meter-${key}`} key={key}><span>{label}</span><div><i style={{width:`${stats[key]}%`}}/></div><b>{Math.round(stats[key])}</b></div>)}<div className="exploration-overrides"><label><input type="checkbox" checked={mistOverride} onChange={event=>onMistOverride(event.target.checked)}/> EXPLORE THE MISTS</label><label><input type="checkbox" checked={fairyOverride} onChange={event=>onFairyOverride(event.target.checked)}/> EXPLORE THE FAIRY PLAYGROUND</label></div></aside>}
